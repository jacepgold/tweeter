const bio = (state = {}, action) => {
    switch (action.type) {
      case :
        return action.user;
      case :
        return {};
      default:
        return state;
    }
  };
  
  export default bio;